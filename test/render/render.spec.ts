import Plusnew from 'index';

describe('rendering the elements', () => {
  let plusnew: Plusnew;
  beforeEach(() => {
    plusnew = new Plusnew();
  });

  it('check if element is inserted', () => {

  });
});
